const progress1 = document.querySelector('.progress-done1');

setTimeout(() => {
    progress1.style.opacity = 1;
    progress1.style.width = progress1.getAttribute('data-done1') + "%"; 
}, 50)

const progress2 = document.querySelector('.progress-done2');

setTimeout(() => {
    progress2.style.opacity = 1;
    progress2.style.width = progress2.getAttribute('data-done2') + "%"; 
}, 50)

const progress3 = document.querySelector('.progress-done3');

setTimeout(() => {
    progress3.style.opacity = 1;
    progress3.style.width = progress3.getAttribute('data-done3') + "%"; 
}, 50)

const progress4 = document.querySelector('.progress-done4');

setTimeout(() => {
    progress4.style.opacity = 1;
    progress4.style.width = progress4.getAttribute('data-done4') + "%"; 
}, 50)

const progress5 = document.querySelector('.progress-done5');

setTimeout(() => {
    progress5.style.opacity = 1;
    progress5.style.width = progress5.getAttribute('data-done5') + "%"; 
}, 50)

const progress6 = document.querySelector('.progress-done6');

setTimeout(() => {
    progress6.style.opacity = 1;
    progress6.style.width = progress6.getAttribute('data-done6') + "%"; 
}, 50)


const progress1x = document.querySelector('.progress-done1x');

setTimeout(() => {
    progress1x.style.opacity = 1;
    progress1x.style.width = progress1x.getAttribute('data-done1x') + "%"; 
}, 50)

const progress2x = document.querySelector('.progress-done2x');

setTimeout(() => {
    progress2x.style.opacity = 1;
    progress2x.style.width = progress2x.getAttribute('data-done2x') + "%"; 
}, 50)

const progress3x = document.querySelector('.progress-done3x');

setTimeout(() => {
    progress3x.style.opacity = 1;
    progress3x.style.width = progress3x.getAttribute('data-done3x') + "%"; 
}, 50)

const progress4x = document.querySelector('.progress-done4x');

setTimeout(() => {
    progress4x.style.opacity = 1;
    progress4x.style.width = progress4x.getAttribute('data-done4x') + "%"; 
}, 50)

const progress5x = document.querySelector('.progress-done5x');

setTimeout(() => {
    progress5x.style.opacity = 1;
    progress5x.style.width = progress5x.getAttribute('data-done5x') + "%"; 
}, 50)

const progress6x = document.querySelector('.progress-done6x');

setTimeout(() => {
    progress6x.style.opacity = 1;
    progress6x.style.width = progress6x.getAttribute('data-done6x') + "%"; 
}, 50)
